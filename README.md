# flip-clock

A simple flip-style clock UI component/application.

## Overview

This project displays the current time using a split-flap / flip-clock visual style. It can be used as a standalone clock page or embedded as a component in a larger application.

## Installation

1. Clone this repository:
   ```bash
   git clone <REPO_URL>
   cd flip-clock